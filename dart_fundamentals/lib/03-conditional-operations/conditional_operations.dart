void main(List<String> args) {
  // Operações de condiocionais
  
  /**
   * Sintaxe
   * if (condição) {
   *  Operação caso a condição seja verdadeira (true)
   * }
   */

  bool isAlive = true;
  // Forma completa
  if (isAlive) {
    print("Está vivo!");
  }

  // Forma reduzida - Para uma única condição
  if (isAlive) print("Está vivo!");

  /**
   * Sintaxe
   * if (condição) {
   *  Operação caso a condição seja verdadeira (true)
   * } else {
   *  Operação caso a condição não seja verdadeira (false)
   * }
   */

//   isAlive = false;
//   if (isAlive) {
//     print("Está vivo!");
//   } else {
//     print("Está Morto!");
//   }
}